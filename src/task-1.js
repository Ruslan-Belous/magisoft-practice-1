export default function isTriangle(a, b, c) {
    return (a + b <= c || a + c <= b || b + c <= a) ? false : true
    // if (a + b <= c || a + c <= b || b + c <= a) {
    //     return false
    // }
    // else {
    //  return 
}
